kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: podinfo
spec:
  to:
    kind: Service
    name: podinfo
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
